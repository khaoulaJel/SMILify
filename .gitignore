# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Ensure no nested __pycache__ folders are tracked
**/__pycache__/

# C extensions
*.so

# blender backup files
*.blend1

# Temp results
Fitter_RESULTS/
fit3d_results
fit3d_results_*/
output/

# third-party tool cloned as a subdirectory with its own .git — not a submodule,
# must never be swept into this repo's index (git warns "adding embedded git
# repository" if it is)
custom_processing/external/Manifold/
custom_processing/external/ManifoldPlus/
custom_processing/external/instant-mesh-intersection-repair/
custom_processing/external/torch-mesh-intersection/
custom_processing/external/torch-mesh-isect/
custom_processing/external/torch-mesh-isect-wonjongg/
# vendored Blender site-packages (numpy/scipy/pillow/trimesh/... bundled for Blender's
# own Python) — 364M of third-party code, not project code
custom_processing/external/blender_python_packages/
# vendored CGAL source tree + local build dir (~560M), not project code
custom_processing/external/cgal_alpha_wrap/
3D_model_prep/morph_anim/
3D_model_prep/measurements/
3D_model_prep/VIDEOS/
3D_model_prep/stick_data/
3D_model_prep/stick_tex/
3D_model_prep/temp_models/
3D_model_prep/COMPARE.blend
3D_model_prep/SMIL_processing_addon_MOUSE.blend
3D_model_prep/SMIL_Stick_insects.blend
3D_model_prep/stick_tex.zip
sdf_batch_output/
sample_output/
plots/
visualizations/
visualizations_train/
test_visualizations/
tests/test_output/
checkpoints/

# unless specifically added, ignore videos
*.mp4

# ignore partial SMPL file contens
*.npy

# ============================================================
# Third-party licensed models — NEVER commit these (see issue #84)
# ============================================================
# The SMPL body model (MPI) and the SMAL/SMALST priors derived from it are
# distributed under research licences that do not permit redistribution. This
# repo is MIT-licensed, so these must be obtained by each user directly from
# the upstream sources. See docs/THIRD_PARTY_MODELS.md.
#
# NOTE: these patterns are deliberately specific. Our own parametric models
# (3D_model_prep/SMPL_fit*.pkl, smpl_ATTA.pkl, SMPL_STICK.pkl, SMIL*/SMILy*)
# use the SMPL *file format* and naming prefix but are our own work — they are
# NOT covered here and remain tracked.
smal_model/smpl/
data/priors/walking_toy_symmetric_pose_prior*.pkl
data/priors/zebra_walking_symmetric_pose_prior*.pkl

# All models
fitter_3d/ALL_ANTS/
fitter_3d/ATTA_ALL/
fitter_3d/DOG_BOI/
fitter_3d/STICKS/
fitter_3d/MOUSE_BOY/
fitter_3d/STICK_TESTS/

# datasets
data/BADJA/
data/SMALST/
data/StanfordExtra/
LOCAL_TEST/

#scraped data
custom_processing/antscan_data/
custom_processing/antscan_processed/
custom_processing/antscan_stats/
custom_processing/antscan_proofread/
custom_processing/antscan_proofread_castes/
custom_processing/proof_read_WIP.blend
custom_processing/proof_read_WIP.blend1

#pycharm dev
.idea/
.vscode/
.cursor/
__pycache__/
fitter_3d/__pycache__/


# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/

# benchmark outputs
benchmark_*/

# custom
dev_notes/
checkpoints/
smal/
extra_videos/
exported/
smpl_webuser/
download/

# scratch diagnostics (probes, smoke HDF5s, ad-hoc inspectors)
probe_*.py
inspect_smoke*.py
roundtrip_smoke*.py
SMOKE_*.h5
SMOKE_*.json
SMOKE_*/
*_SMOKE_TEST.json
# cross-source merger scratch — smoke outputs from merge_multiview_datasets.py
# and the SLEAP canonicalization prototype's diagnostic plot.
merged_*.h5
sleap_canonicalize_proto*.png

# ============================================================
# Generated data, model weights, and run outputs (none tracked)
# ============================================================
# Datasets, triangulation/reprojection outputs, SLEAP exports.
# No .h5 is version-controlled; all are regenerable pipeline data.
*.h5
# Model weights / checkpoints (e.g. best_model.pth, *_final_model.pth).
*.pth
# Archives.
*.zip

# Multi-view training run outputs — these patterns match at any depth,
# so both top-level dirs and per-model-run subdirs are covered
# (e.g. ViT_Large_*/multiview_checkpoints/).
multiview_checkpoints*/
multiview_plots*/
multiview_singleview_renders*/
multiview_visualizations*/

# Single-view / getting-started training run outputs (checkpoints, plots,
# visualizations). Regenerable; covers getting_started_* and
# getting_started_singleview_* run dirs.
getting_started*checkpoints/
getting_started*plots/
getting_started*visualizations*/
benchmark_singleview_*/

# Scratch parametric-model exports from Blender prep. The tracked .pkl
# models carry no _TEST suffix, so they are unaffected.
3D_model_prep/*_TEST*.pkl

# Claude Code: project CLAUDE.md is tracked; machine-local settings are not.
.claude/settings.local.json

# ============================================================
# diagnostics/ investigation: large regenerable fit output + local dataset
# mirrors (2026-08-13). Fit runs are multi-GB of regenerable .npz per
# specimen (same reasoning as fit3d_results/ above); dataset mirror dirs are
# symlinks into machine-local paths (/media/fabi/Data/... or this cluster's
# /p/scratch/...) that are not portable and would just be broken links in
# anyone else's checkout. Small result artifacts (out/ dirs: JSON/CSV/PNG)
# are NOT covered here and remain tracked.
# ============================================================
diagnostics/moonshot/runs/
diagnostics/khaoula_v2/runs/
diagnostics/khaoula_v2/out/
diagnostics/moonshot/clean81/
diagnostics/moonshot/coreg_train/
diagnostics/moonshot/coreg_test/
diagnostics/moonshot/heldout_rand/
diagnostics/moonshot/heldout_easy/
diagnostics/moonshot/heldout_easy2/
diagnostics/khaoula_review/smoke_mesh_dir/
diagnostics/khaoula_review/smoke_offset/
diagnostics/morphometrics/ab_scale_cap/smoke_D1_PROD_mesh/
diagnostics/morphometrics/ab_scale_cap/smoke_D1_PROD_out/
diagnostics/morphometrics/ab_scale_cap/stage_A/
diagnostics/morphometrics/ab_scale_cap/stage_B/
diagnostics/morphometrics/stage/

# Large regenerable training corpora (5000-specimen synthetic datasets,
# ~550M each) for the anatomical-pose-init investigation. Regenerate via
# generate_synth_5000*.slurm + diagnostics/moonshot/generate_synth_large.py.
diagnostics/moonshot/synth_large/
diagnostics/moonshot/synth_large_25pc/
diagnostics/moonshot/synth_large_25pc_lowpose/
diagnostics/moonshot/synth_large_25pc_lowpose05/
diagnostics/moonshot/synth_large_25pc_lowpose10/

# Large regenerable B2 training corpus (236M) for the anatomical-pose-init
# investigation. Regenerate via make_b2_training_corpus_20260825.py (see
# its --out default for the path).
diagnostics/moonshot/synth_b2_train_corrb06.npz

# issue95_fullrun: pure scratch (downloaded raw STLs mid-processing) and
# reprocessed specimen outputs already uploaded to the Google Drive
# destination (antscan_processed_issue95) -- not source-of-truth here.
diagnostics/issue95_fullrun/tmp/
diagnostics/issue95_fullrun/specimens/
